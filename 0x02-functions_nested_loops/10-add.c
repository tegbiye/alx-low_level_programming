#include "main.h"

/**
 * add - Adds two integers and returns the result
 *@a: number being added
 *@b: number being added
 * Return: Always 0.
 */

int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
