#include "main.h"
#include <string.h>

/**
 * _strlen - a function that returns the length of a string
 * @s: input character 1
 *
 * Return: length of string
 */

int _strlen(char *s)
{
return (strlen(s));
}
